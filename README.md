pip install -r requirements.txt

1. create_dataset.py = ใช้สร้าง dataset จากกล้อง ให้เปิด comment คลาสที่ต้องการสร้าง dataset แล้วรันไฟล์นี้
2. create_augment_data = augmentation ขยายข้อมูลรวมกับรูปออริ
3. create_csv.py = ใช้แปลงไฟล์ภาพเป็น csv


main.py = รันโค้ดหลัก


